opacity-rule = [
	"90:name = 'st' && focused",
	"65:name = 'st' && !focused",
	"85:name = 'rofi'",
	"90:name = 'mupdf' && focused",
	"65:name = 'mupdf' && !focused",
	"100:name = 'firefox'"
];

wintypes:
{
	tooltip = { fade = true; shadow = false; };
	menu = { shadow = false; };
	dropdown_menu = { shadow = false; };
	popup_menu =  { shadow = false; };
}
